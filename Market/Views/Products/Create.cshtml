@using Market.Models;
@model Product

<h3>Продукт</h3>

@using (Html.BeginForm("Create", "Products", FormMethod.Post))
{
    @Html.EditorForModel()
    <input type="submit" value="Сохранить" />
    @Html.ActionLink("Отменить", "Index", "Products")
}